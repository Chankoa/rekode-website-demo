[build]
  command = "npm ci --include=optional || npm install --include=optional; npm install --no-save @rollup/rollup-linux-x64-gnu@4.61.0; npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.12.0"
  NPM_FLAGS = "--include=optional"
